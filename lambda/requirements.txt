# Lambda package dependencies
# boto3 is provided by the Lambda runtime — do NOT include it here.
# Versions aligned with backend/pyproject.toml.

pydantic>=2.10
pydantic-settings>=2.7
httpx>=0.28
python-dateutil>=2.9
python-dotenv>=1.0
